[package]
name = "conformance-adapter-common"
version = "0.1.0"
edition = "2021"
publish = false
description = "Shared vocabulary for conformance adapters: outcomes, the result document, the corpus registry, and the corpus runner"

[dependencies]
anyhow = { workspace = true }
futures = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = "0.10"
tokio = { workspace = true, features = ["time", "net"] }
toml = { workspace = true }
