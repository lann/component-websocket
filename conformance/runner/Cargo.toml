[package]
name = "conformance-runner"
version = "0.1.0"
edition = "2021"
publish = false
description = "Classifies adapter result documents against the registry and target facts, and renders the conformance matrix"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
